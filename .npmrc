# The public npm registry (registry.npmjs.org) is not reachable from the
# Autodesk corporate network; the Artifactory virtual registry mirrors it.
# Scoped to this project so it does not alter global npm configuration.
registry=https://art-bobcat.autodesk.com/artifactory/api/npm/autodesk-npm-virtual/
